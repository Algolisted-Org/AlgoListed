//write all middlewares here
/*
const { json } = require("express");
const checkAdmin = (req,res,next)=>{
    if(req.body.admin != "lottle-admins03") res.status(400).json("Access Denied !!!") ; 
    next() ;
}

module.exports = {
    checkAdmin
} */